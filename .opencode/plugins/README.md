# Set 1 Plugins

No project plugin is enabled. This directory is reserved for reviewed Set 1 plugins only.

Adding a plugin or dependency requires explicit approval, source review, a pinned version where applicable, and validation that native OpenCode permissions cannot provide the same safeguard.
